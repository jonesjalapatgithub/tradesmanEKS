spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://tradesman.ctmy5ztkjoww.us-east-2.rds.amazonaws.com:5432/postgres
    password: password
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
logging:
  level:
    liquibase: INFO


bucketname : tradesman-bucket
region : us-east-2